/// Defines the type of flash message to be displayed
enum FlashMessageType {
  /// Success message type (typically green)
  success,

  /// Error message type (typically red)
  error,

  /// Information message type (typically blue)
  info,

  /// Warning message type (typically orange)
  warning
}
